<!-- Validation failure messages -->
<% if @category.errors.any? %>
  <% @category.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>


<div class="row">
  <div class="col-md-12">
    <form action="/update_category/<%= @category.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for club_id -->
      <div class="form-group">
        <label for="club_id" class="control-label">
          Club
        </label>

        <input type="text" id="club_id" name="club_id" placeholder="Club" class="form-control" value="<%= @category.club_id %>">
      </div>

      <!-- Label and input for name -->
      <div class="form-group">
        <label for="name" class="control-label">
          Name
        </label>

        <input type="text" id="name" name="name" placeholder="Name" class="form-control" value="<%= @category.name %>">
      </div>

      <button class="btn btn-warning">
        Update Category
      </button>
      or
      <a href="/categories/<%= @category.id %>">Cancel</a>
    </form>
  </div>
</div>
